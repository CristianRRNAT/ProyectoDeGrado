@extends('admin.layout') @section('title','Resumen') @section('heading','Resumen general') @section('content')
<section class="stat-grid">@foreach($stats as $label=>$value)<article><span>{{ $label }}</span><strong>{{ $value }}</strong><i class="bi bi-arrow-up-right"></i></article>@endforeach</section><section class="admin-columns"><article class="panel"><div class="panel-title"><div><span>REVISIÓN</span><h2>Solicitudes recientes</h2></div><a href="{{ route('admin.submissions') }}">Ver todas</a></div>@forelse($submissions as $item)<div class="activity"><i class="bi bi-inbox"></i><div><b>{{ $item->organization_name }}</b><span>{{ $item->full_name }} · {{ ucfirst($item->opportunity_type) }}</span></div><em class="status {{ $item->status }}">{{ $item->status }}</em></div>@empty<p class="empty">No hay solicitudes.</p>@endforelse</article><article class="panel"><div class="panel-title"><div><span>COMUNIDAD</span><h2>Comentarios recientes</h2></div><a href="{{ route('admin.comments') }}">Moderar</a></div>@forelse($comments as $item)<div class="activity"><i class="bi bi-chat"></i><div><b>{{ $item->user->name }}</b><span>{{ Str::limit($item->content,60) }}</span></div><em class="status {{ $item->status }}">{{ $item->status }}</em></div>@empty<p class="empty">No hay comentarios.</p>@endforelse</article></section>
@endsection
